# Optional live SEC refresh; replace the example address with your real contact.
# The CLI reads process environment variables. It does not load this file automatically.
SEC_USER_AGENT=CreditLake your.name@example.com
