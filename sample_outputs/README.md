# Executed sample outputs

These CSV and Parquet marts were generated from the bundled real SEC responses by the validated fresh pipeline. `dbt/` contains its actual model graph and quality test results. Recreate them with `creditlake run`. Missing financial metrics remain null.
